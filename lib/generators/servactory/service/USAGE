Description:
    Generates a new Servactory service class.

Usage:
    rails generate servactory:service NAME [inputs] [options]

Arguments:
    NAME           The name of the service (e.g., Users::Create, ProcessOrder)
    inputs         Input attributes with optional types (e.g., email:String count:Integer)

Options:
    --base-class=CLASS    Base class for the service (default: ApplicationService::Base)
    --skip-output         Skip default output declaration
    --skip-make           Skip default make method
    --internal=ATTRS      Internal attributes (e.g., --internal=cache data)
    --output=ATTRS        Output attributes (e.g., --output=result status)

Examples:
    rails generate servactory:service Users::Create
        Creates app/services/users/create.rb with default structure

    rails generate servactory:service Users::Create email:String name:String
        Creates service with typed email and name inputs

    rails generate servactory:service ProcessOrder order_id:Integer --output=receipt status
        Creates service with order_id input and receipt, status outputs

    rails generate servactory:service SendEmail recipient:String --skip-make
        Creates service without default make method

    rails generate servactory:service CacheData key:String --internal=cached_value
        Creates service with cached_value internal attribute
