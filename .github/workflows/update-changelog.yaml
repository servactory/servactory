name: Update Changelog

on:
  release:
    types:
      - released

jobs:
  update:
    runs-on: ubuntu-latest

    permissions:
      # Give the default GITHUB_TOKEN write permission to commit and push the
      # updaetd CHANGELOG back to the repository.
      # https://github.blog/changelog/2023-02-02-github-actions-updating-the-default-github_token-permissions-to-read-only/
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          ref: ${{ github.event.release.target_commitish }}

      - name: Update Changelog
        uses: stefanzweifel/changelog-updater-action@v1
        with:
          release-notes: ${{ github.event.release.body }}
          latest-version: ${{ github.event.release.name }}

      - name: Commit updated CONTRIBUTING.md
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          branch: ${{ github.event.release.target_commitish }}
          commit_message: Update CONTRIBUTING.md
          file_pattern: website/docs/CONTRIBUTING.md

#      - name: Commit updated website/i18n/ru/docusaurus-plugin-content-docs/current/CHANGELOG.md
#        uses: stefanzweifel/git-auto-commit-action@v4
#        with:
#          branch: ${{ github.event.release.target_commitish }}
#          commit_message: Update CONTRIBUTING.md
#          file_pattern: website/i18n/ru/docusaurus-plugin-content-docs/current/CHANGELOG.md
