module Servactory
  module Configuration
    module DSL
      def self.included: (untyped base) -> void

      module ClassMethods
        @config: Setup
        @configuration_factory: Factory

        def config: -> Servactory::Configuration::Setup

        private

        def configuration: () { () -> untyped } -> Proc
      end
    end
  end
end
